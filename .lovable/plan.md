

## 將價錢、原價與有效期限合併為同一列

### 變更內容

將目前分開的「價格區塊」和「有效期限」合併為一個 flex row，由左至右排列：

```text
[$199]  [原價 $250]  [📅 2024-01-01 ~ 2024-12-31]
```

### 技術細節

**修改 `src/components/CouponCard.tsx`（第 94~113 行）**：
- 將價格區塊與有效期限合併為一個 `flex items-center` 容器
- 優惠價靠左顯示
- 原價（刪除線）在中間
- 有效期限使用 `ml-auto` 靠右
- 移除原本價格區塊的 `mb-4` 和 `border-t`，改在合併後的容器上套用
- 調整字體大小讓三者在同一列視覺協調（價格稍微縮小為 `text-xl`）

**修改測試 `src/test/components/CouponCard.test.tsx`**：
- 更新相關測試以確保仍能正確找到價格與日期元素

